import type { PartialTranslationResource } from "../types";

// Phase 14A intentionally relies on the English fallback until translations are reviewed.
export const ja = {} as const satisfies PartialTranslationResource;
